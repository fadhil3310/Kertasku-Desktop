import QtQuick 2.15
//import FluentPlugin 1.0

pragma Singleton
Item {
    property color windowColor//: FluentPlugin.micaEnabled ? "transparent" : "white" //Fluent.isLight ? "white" : "black"

    //property color scrollBarBackground
}
